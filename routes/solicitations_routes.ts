import { Router } from "express";

const solicitationsRouter = Router();

//solicitationsRouter.get("/", getSolicitations);
//solicitationsRouter.post("/", newSolicitations);
//solicitationsRouter.post("/:id/accept", newSolicitations);
//solicitationsRouter.post("/:id/reject", newSolicitations);

export default solicitationsRouter;